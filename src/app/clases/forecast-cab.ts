import { ForecastDet } from 'src/app/clases/forecast-det';

export class ForecastCab {
	coServ: string;
	noServ: string;
	coNave: string;
	noNave: string;
	alNave: string;
	detalles: ForecastDet[];
}
